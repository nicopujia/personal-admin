server {
    listen       80;
    server_name  admin.pujia.ar;

    root /var/www/letsencrypt;

    location ^~ /.well-known/acme-challenge/ {
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 301 https://admin.pujia.ar$request_uri;
    }
}

server {
    listen       443 ssl;
    server_name  admin.pujia.ar;

    ssl_certificate      /etc/letsencrypt/live/admin.pujia.ar/fullchain.pem;
    ssl_certificate_key  /etc/letsencrypt/live/admin.pujia.ar/privkey.pem;

    location / {
        auth_basic           "Pujia Admin";
        auth_basic_user_file __ADMIN_HTPASSWD_PATH__;

        proxy_pass         http://127.0.0.1:8043;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
}
